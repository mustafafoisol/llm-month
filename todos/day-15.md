# Day 15 — DocChat Pro (agent + eval)
Sunday, June 14, 2026 · Project 3 of 5

> **Activate your venv first.** Run `source .venv/bin/activate` (Windows: `.venv\Scripts\activate`) before installing or running anything today — your prompt should show `(.venv)`. Do all work inside the venv.

## Today's goal
Re-express the flow as a LangGraph graph.

## Learn first (~30 min)
Stateful graphs: nodes, edges, shared state.

## Tasks
- [ ] Learn LangGraph basics and model the flow as a graph (retrieve -> generate).
- [ ] Define the shared state (question, history, context, answer).
- [ ] Run it and confirm parity with the LCEL version.

## Files you'll create / touch
- `src/project3_docchat_pro/graph.py`

## Definition of done
The RAG flow runs as a LangGraph graph with explicit state.

## If you get stuck (the 30-minute rule)
Stretch goal: if it feels heavy, keep the LCEL chain and revisit later — don't lose the day.

## Notes / scratchpad
_Fill this in as you work: decisions made, blockers hit, useful links._



## End of day
- [ ] Ran inside the **activated venv** (`(.venv)` visible) and `python ...` works
- [ ] Committed to git with a real message
- [ ] Checked off today's tasks above and updated PROGRESS.md
