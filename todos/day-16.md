# Day 16 — DocChat Pro (agent + eval)
Monday, June 15, 2026 · Project 3 of 5

> **Activate your venv first.** Run `source .venv/bin/activate` (Windows: `.venv\Scripts\activate`) before installing or running anything today — your prompt should show `(.venv)`. Do all work inside the venv.

## Today's goal
Turn it into an agent that can use a tool.

## Learn first (~30 min)
Tool/function calling; letting the model decide when to call a tool.

## Tasks
- [ ] Add one tool (web search, a calculator, or a docs-only lookup).
- [ ] Let the agent choose between answering from docs and calling the tool.
- [ ] Test one question that needs the tool and one that doesn't.

## Files you'll create / touch
- `src/project3_docchat_pro/agent.py`

## Definition of done
The agent invokes the tool only when it's actually needed.

## If you get stuck (the 30-minute rule)
A single tool behind a simple router is fine if full agentic tool-choice is finicky.

## Notes / scratchpad
_Fill this in as you work: decisions made, blockers hit, useful links._



## End of day
- [ ] Ran inside the **activated venv** (`(.venv)` visible) and `python ...` works
- [ ] Committed to git with a real message
- [ ] Checked off today's tasks above and updated PROGRESS.md
