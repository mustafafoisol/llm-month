# Project 0 — Launchpad

**Days 1.** Code for this project lives in this folder and grows day by day.

## Goal
Get a working dev environment that calls an LLM and computes embeddings.

## Definition of done
Both `hello_llm.py` and `embed_demo.py` run; related sentences score higher than unrelated ones.

See the matching `todos/day-NN.md` files for the daily task breakdown, and `PLAN.md` for the big picture.
